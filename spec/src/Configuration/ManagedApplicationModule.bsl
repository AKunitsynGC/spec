
Процедура ПриНачалеРаботыСистемы()
	
	ДатаСервера = ВызовСервера.ПолучитьТекущуюДату();
	
	Сообщение = Новый СообщениеПользователю();
	Сообщение.Текст = СтрШаблон("Hello world. Ваше время %1. Время сервера %2", ТекущаяДата(), ДатаСервера);
	Сообщение.Сообщить();
	
	НазваниеЗаполнено = ВызовСервера.ПроверитьКонстанту();
	Если Не НазваниеЗаполнено Тогда
		Сообщить("Введите сведения об организации!");
	КонецЕсли;
	
КонецПроцедуры

